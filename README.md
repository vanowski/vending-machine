# VendingMachine

Simple project emulating a vending machine. It is using an in-memory database mockup for storing item information and a hard-coded set of coins that are initially available. Resources are finite but refillable with user's coins.

## Installation
```
npm install
npm start
```

Server should be running on http://localhost:4200.
